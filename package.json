{
    "scripts": {
        "build": "npm install && sass scss/_custom.scss css/base/base.css",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "bootstrap": "^5.3.0"
    }
}
